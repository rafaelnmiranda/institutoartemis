# SMTP — Gmail / Google Workspace (App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rafa@byutmb.com.br
SMTP_PASS=sua-app-password-aqui

# Destino do formulário de contato
CONTACT_TO=rafa@byutmb.com.br

# Admin (painel em /admin — sem link público)
ADMIN_EMAIL=rafa@byutmb.com.br
ADMIN_PASSWORD=admin

# Vercel Blob (produção — uploads de imagens e PDFs)
# Crie um Blob store no dashboard Vercel e copie o token
BLOB_READ_WRITE_TOKEN=
